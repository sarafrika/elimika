/**
 * Wallet module manages user wallet balances and ledger entries for deposits, sales, and transfers.
 */
@org.springframework.modulith.ApplicationModule(
        allowedDependencies = {
                "shared"
        }
)
package apps.sarafrika.elimika.wallet;
