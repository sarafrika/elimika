/**
 * Read-only timetabling lookup SPI exposed by the shared module.
 * <p>
 * Provides schedule projections that other modules can consume without depending
 * on the timetabling module directly, preventing cyclic dependencies.
 */
package apps.sarafrika.elimika.shared.spi.timetabling;
