package apps.sarafrika.elimika.shared.enums;

/**
 * Defines whether a class is accessible to the general public or restricted to private audiences.
 */
public enum ClassVisibility {
    PUBLIC,
    PRIVATE
}
