package apps.sarafrika.elimika.shared.enums;

/**
 * Preset service offering a class advert is posted under. Complements {@link SessionFormat}
 * (which only captures individual vs group) with the organisation's chosen commercial format.
 */
public enum ClassServiceType {
    ONE_ON_ONE,
    GROUP,
    ONLINE,
    PRIVATE_ONLINE
}
