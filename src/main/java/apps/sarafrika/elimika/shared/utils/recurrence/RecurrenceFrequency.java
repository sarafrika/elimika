package apps.sarafrika.elimika.shared.utils.recurrence;

/**
 * Supported recurrence frequencies for session template expansion.
 */
public enum RecurrenceFrequency {
    DAILY,
    WEEKLY,
    MONTHLY
}
