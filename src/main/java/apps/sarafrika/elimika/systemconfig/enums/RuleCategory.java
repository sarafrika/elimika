package apps.sarafrika.elimika.systemconfig.enums;

/**
 * Logical groupings of rule intents so downstream modules can query the correct policies.
 */
public enum RuleCategory {
    PLATFORM_FEE,
    AGE_GATE,
    ENROLLMENT_GUARD,
    CUSTOM
}
