package apps.sarafrika.elimika.systemconfig.enums;

/**
 * Provides hints about how to interpret {@code value_payload}.
 */
public enum RuleValueType {
    JSON,
    DECIMAL,
    INTEGER,
    BOOLEAN,
    STRING
}
